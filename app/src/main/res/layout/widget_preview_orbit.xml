<?xml version="1.0" encoding="utf-8"?>
<!--
    Picker preview for the Orbit widget (1x1): round cover with the hero
    centred on it. The hero sits on a dark scrim circle, like the widget.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        android:src="@drawable/widget_preview_art_circle" />

    <FrameLayout
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="center"
        android:background="@drawable/widget_preview_scrim">

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="center"
            android:contentDescription="@string/widget_action_play"
            android:src="@drawable/ic_media_play" />
    </FrameLayout>
</FrameLayout>
