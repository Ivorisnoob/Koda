<?xml version="1.0" encoding="utf-8"?>
<!--
    2x4 vertical player: squircle cover, metadata, progress, full transport, shuffle and repeat.

    MusicService pushes a redraw on each playback event; the 30-minute
    updatePeriodMillis heartbeat (the platform floor) exists only so a widget
    frozen by an OEM force-killing the app eventually corrects itself.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="150dp"
    android:minHeight="250dp"
    android:minResizeWidth="150dp"
    android:minResizeHeight="250dp"
    android:targetCellWidth="2"
    android:targetCellHeight="4"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="1800000"
    android:description="@string/widget_deck_description"
    android:previewLayout="@layout/widget_preview_deck"
    android:previewImage="@drawable/widget_preview_deck_image"
    android:initialLayout="@layout/glance_default_loading_layout" />
