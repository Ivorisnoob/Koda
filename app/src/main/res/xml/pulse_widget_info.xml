<?xml version="1.0" encoding="utf-8"?>
<!--
    4x1 that grows into a 4x2 card: cookie-cut cover, track, hero; adds progress and a transport group when made taller.

    MusicService pushes a redraw on each playback event; the 30-minute
    updatePeriodMillis heartbeat (the platform floor) exists only so a widget
    frozen by an OEM force-killing the app eventually corrects itself.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="60dp"
    android:minResizeWidth="250dp"
    android:minResizeHeight="60dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="1800000"
    android:description="@string/widget_pulse_description"
    android:previewLayout="@layout/widget_preview_pulse"
    android:previewImage="@drawable/widget_preview_pulse_image"
    android:initialLayout="@layout/glance_default_loading_layout" />
