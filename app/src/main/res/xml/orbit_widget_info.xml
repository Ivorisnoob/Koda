<?xml version="1.0" encoding="utf-8"?>
<!--
    1x1 round cover with a play button centred on it.

    MusicService pushes a redraw on each playback event; the 30-minute
    updatePeriodMillis heartbeat (the platform floor) exists only so a widget
    frozen by an OEM force-killing the app eventually corrects itself.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="90dp"
    android:minHeight="90dp"
    android:minResizeWidth="90dp"
    android:minResizeHeight="90dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:resizeMode="none"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="1800000"
    android:description="@string/widget_orbit_description"
    android:previewLayout="@layout/widget_preview_orbit"
    android:previewImage="@drawable/widget_preview_orbit_image"
    android:initialLayout="@layout/glance_default_loading_layout" />
