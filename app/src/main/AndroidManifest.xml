<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!--
        Android 14 lets someone grant access to a chosen subset of their media
        rather than all of it. Without this the picker offers no such choice and
        the only answers are "all videos" or "none"; with it, a partial grant
        denies READ_MEDIA_VIDEO while still returning the selected files, which
        LocalVideoAccess.PARTIAL is what surfaces.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Metered-network detection for the per-network stream quality settings -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 16+ Live Updates (Live Activities) -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".IvorMusicApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="${appLabel}"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.App.Starting">
        <!--
            singleTask so a link or a video file handed to Koda is delivered to
            the running instance through onNewIntent, instead of stacking a
            second MainActivity on top of the live players.

            singleTop was not enough, and picture-in-picture is why: PiP moves
            the activity into a task of its own, so an "open with" arriving from
            a file manager - which launches into a new task - matched nothing
            and started a second MainActivity. Two instances meant two
            VideoPlayerViewModels and two ExoPlayers: the PiP window carried on
            playing while the new copy opened the file and lost the audio focus
            fight, which is the "it opens but does not play" case. singleTask
            routes that launch to the existing instance and expands it out of
            PiP. Koda is a single-activity app, so being the root of its task
            costs nothing.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="${appLabel}"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.App.Starting"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Android's share sheet. A mime type is the only filter the
                manifest can express, so Koda is offered for any shared text and
                sorts out whether it holds a YouTube link at handling time.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>

            <!--
                "Open with" for YouTube links. Paths are constrained to what the
                app can actually play, so Koda is not offered for channel or
                account URLs it would only fail on. music.youtube.com links open
                in the music player, the rest in the video player.
            -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="youtu.be" />
                <data android:pathPattern="/.*" />
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="youtube.com" />
                <data android:host="www.youtube.com" />
                <data android:host="m.youtube.com" />
                <data android:host="music.youtube.com" />
                <data android:pathPrefix="/watch" />
                <data android:pathPrefix="/playlist" />
                <data android:pathPrefix="/shorts/" />
                <data android:pathPrefix="/live/" />
                <data android:pathPrefix="/embed/" />
                <data android:pathPrefix="/v/" />
            </intent-filter>

            <!--
                "Open with" for video files, so Koda is offered alongside the
                gallery and other players. Both halves are needed: a file
                manager sends ACTION_VIEW with the file in the data URI, while a
                gallery's share sheet sends ACTION_SEND with it in EXTRA_STREAM.

                The filters match by mime type only. Matching by extension as
                well would need a pathPattern per container and would still miss
                the many providers that serve a content URI with no name at all,
                so the intent is checked again at handling time
                (Intent.externalVideoUri) rather than trusted from the filter.
            -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>
        
        <meta-data android:name="com.google.android.gms.car.application" android:resource="@xml/automotive_app_desc"/>

        <service
            android:name=".service.MusicService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService"/>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>

        <!--
            Puts video playback on the lock screen, in the shade's media area
            and on Bluetooth/headset transport buttons. The ExoPlayer itself
            stays owned by VideoPlayerViewModel; this service only wraps it in
            a MediaSession, which is also what makes background video audio a
            legitimate foreground service rather than an unannounced one.
        -->
        <service
            android:name=".service.VideoPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService"/>
            </intent-filter>
        </service>

        <!--
            Keeps the process alive while downloads run. dataSync is the correct
            type for user-initiated file transfers; the matching
            FOREGROUND_SERVICE_DATA_SYNC permission is declared above.
        -->
        <service
            android:name=".service.DownloadService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!--
            Quick Settings playback tile. A pure session client: it binds a
            MediaController to MusicService, so the tile's play/pause and its
            state are the same playback the app shows. The BIND_QUICK_SETTINGS
            _TILE permission is the system's own requirement for talking to it;
            exported is mandatory for the same reason.
        -->
        <service
            android:name=".service.PlaybackTileService"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!--
            The home screen widget family. Six providers over one observable
            snapshot (widget/PlayerWidgetHost.kt), all pure session clients like
            the QS tile: none holds a MediaController, and every tap binds a
            throwaway one on the main thread.

            updatePeriodMillis is 0 on all of them because MusicService pushes a
            redraw on each playback event; scheduled polling would only burn
            battery redrawing the same picture.

            Listed in picker order - the everyday one first, the two decorative
            ones last.
        -->
        <receiver
            android:name=".widget.PulseWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_pulse_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/pulse_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.ConsoleWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_console_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/console_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.DeckWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_deck_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/deck_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.LineupWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_lineup_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lineup_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.BloomWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_bloom_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bloom_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.OrbitWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_orbit_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/orbit_widget_info" />
        </receiver>

    </application>

</manifest>
